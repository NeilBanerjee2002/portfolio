{
  "name": "portfolio-project",
  "version": "1.0.0",
  "scripts": {
    "frontend": "cd front-end && npm run dev",
    "backend": "cd back-end && node server.js",
    "dev": "concurrently \"npm run backend\" \"npm run frontend\""
  },
  "devDependencies": {
    "concurrently": "^9.0.0"
  },
  "dependencies": {
    "bootstrap": "^5.3.8",
    "express-rate-limit": "^8.5.1"
  }
}
